[build]
  publish   = "frontend/dist"
  command   = "bash ./netlify_build.sh; (cd backend/netlify_functions; npm ci)"
  functions = "backend/netlify_functions"

[[redirects]]
  from   = "/api/v0/*"
  to     = "/.netlify/functions/:splat"
  status = 200
